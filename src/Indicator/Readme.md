Indicator renders a green circle if status prop is "ready" or an orange one if is "awaiting".
Default status will be null with no background color.

```jsx
<Indicator status="ready" />
```

```jsx
<Indicator status="awaiting" />
```
